#include "main.h"

/**
 * print_numbers(void): prints the numbers, from 0 to 9, followed by a new line.
 */
void print_numbers(void){
	int i=48;
	for(;i<58;i++)
		_putchar(i);
	_putchar(10);
	return(0);
}
